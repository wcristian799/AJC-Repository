{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "tanstack-start",
  "installCommand": "bun install --frozen-lockfile --force --no-cache",
  "buildCommand": "rm -rf node_modules/.nitro .vercel/output && bun run build"
}
